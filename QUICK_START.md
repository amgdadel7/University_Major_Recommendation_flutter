# 🚀 دليل البدء السريع - Quick Start Guide

## ⚡ للبدء الفوري

### الخطوة 1: التأكد من تثبيت Flutter
```bash
flutter --version
```

إذا لم يكن مثبتاً، قم بتحميله من: https://flutter.dev

### الخطوة 2: تثبيت المكتبات
```bash
flutter pub get
```

### الخطوة 3: تشغيل التطبيق
```bash
flutter run
```

## 🎯 اختبار الميزات

### 1. اختبار تغيير اللغة
1. افتح التطبيق
2. اضغط على أيقونة اللغة 🌐 في الشريط العلوي
3. اختر بين العربية والإنجليزية
4. لاحظ تغيير اتجاه الواجهة تلقائياً

### 2. اختبار تغيير الثيم
1. اضغط على أيقونة الثيم (شمس/قمر) في الشريط العلوي
2. لاحظ التبديل بين الوضع الفاتح والداكن
3. أو اذهب إلى الإعدادات > الثيم للمزيد من الخيارات

### 3. اختبار التنقل
1. استخدم شريط التنقل السفلي:
   - 🏠 الرئيسية
   - ⭐ التوصيات
   - 🎓 الجامعات
   - 📄 الطلبات
   - 👤 الملف الشخصي

### 4. اختبار التقييم
1. من الشاشة الرئيسية، اضغط "ابدأ التقييم"
2. أدخل درجاتك
3. أكمل الاستبيانات الثلاثة:
   - الاهتمامات
   - الأهداف
   - أسلوب التعلم
4. اعرض التوصيات

### 5. اختبار التقديم
1. اذهب إلى الجامعات
2. اختر جامعة
3. اضغط "قدّم الآن"
4. املأ النموذج متعدد الخطوات
5. أرسل الطلب

## 📱 اختبار على أجهزة مختلفة

### Android Emulator
```bash
# إنشاء محاكي
flutter emulators --create

# عرض المحاكيات المتاحة
flutter emulators

# تشغيل التطبيق
flutter run
```

### iOS Simulator (macOS only)
```bash
# فتح المحاكي
open -a Simulator

# تشغيل التطبيق
flutter run
```

### Chrome (للاختبار السريع)
```bash
flutter run -d chrome
```

## 🎨 تخصيص سريع

### تغيير اللون الأساسي
افتح `lib/core/theme/app_colors.dart`:

```dart
// Light Theme
static const Color primaryLight = Color(0xFF6366F1); // غير هذا
```

### إضافة نص جديد
افتح `assets/translations/ar.json`:

```json
{
  "your_key": "النص بالعربية"
}
```

افتح `assets/translations/en.json`:

```json
{
  "your_key": "Text in English"
}
```

استخدمه في الكود:

```dart
Text('your_key'.tr())
```

## 🔧 حل المشاكل السريع

### المشكلة: الترجمات لا تظهر
```bash
flutter clean
flutter pub get
flutter run
```

### المشكلة: أخطاء في البناء
```bash
flutter clean
flutter pub cache repair
flutter pub get
```

### المشكلة: التطبيق بطيء
```bash
# استخدم وضع Release
flutter run --release
```

## 📊 بنية المشروع

```
lib/
├── main.dart                    # نقطة البداية
├── core/
│   ├── app.dart                 # التطبيق الرئيسي
│   ├── theme/                   # الثيمات والألوان
│   └── router/                  # التنقل
└── features/                    # الميزات
    ├── auth/                    # التسجيل والدخول
    ├── home/                    # الشاشة الرئيسية
    ├── grades/                  # الدرجات
    ├── survey/                  # الاستبيانات
    ├── recommendations/         # التوصيات
    ├── universities/            # الجامعات
    ├── applications/            # الطلبات
    └── profile/                 # الملف الشخصي
```

## 🎯 الميزات الرئيسية

✅ **14 شاشة** كاملة ومتكاملة
✅ **لغتان** (العربية والإنجليزية)
✅ **وضعان** (فاتح وداكن)
✅ **رسوم متحركة** سلسة
✅ **تصميم عصري** احترافي
✅ **كود نظيف** ومنظم

## 📱 متطلبات التشغيل

### الحد الأدنى:
- Flutter 3.0.0 أو أحدث
- Dart 3.0.0 أو أحدث

### الموصى به:
- Flutter 3.13.0 أو أحدث
- VS Code أو Android Studio
- جهاز/محاكي للاختبار

## 🚀 البناء للإنتاج

### Android APK
```bash
flutter build apk --release
```
الملف في: `build/app/outputs/flutter-apk/app-release.apk`

### Android App Bundle
```bash
flutter build appbundle --release
```
الملف في: `build/app/outputs/bundle/release/app-release.aab`

### iOS
```bash
flutter build ios --release
```

## 📝 ملاحظات مهمة

1. **الخطوط:**
   - التطبيق يستخدم Google Fonts
   - لا حاجة لتحميل خطوط يدوياً

2. **الصور:**
   - الصور الحالية placeholders
   - استبدلها بصور حقيقية في `assets/images/`

3. **البيانات:**
   - البيانات الحالية Mock Data
   - اربطها بـ Backend حقيقي

## 🎓 موارد التعلم

- [Flutter Docs](https://docs.flutter.dev)
- [Material Design](https://m3.material.io)
- [BLoC Pattern](https://bloclibrary.dev)

## 💡 نصائح مفيدة

### Hot Reload
- اضغط `r` في Terminal للتحديث السريع
- اضغط `R` لإعادة التشغيل

### DevTools
```bash
flutter pub global activate devtools
flutter pub global run devtools
```

### تحليل الكود
```bash
flutter analyze
```

### الاختبارات
```bash
flutter test
```

## 📞 الدعم

لأي استفسارات أو مشاكل:
- راجع [SETUP_GUIDE.md](SETUP_GUIDE.md) للتفاصيل
- راجع [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) للنظرة العامة
- افتح Issue على GitHub

## ✅ قائمة التحقق

قبل البدء، تأكد من:
- [ ] تثبيت Flutter
- [ ] تشغيل `flutter doctor`
- [ ] تشغيل `flutter pub get`
- [ ] توفر جهاز/محاكي للاختبار

## 🎉 ابدأ الآن!

```bash
# افتح مجلد المشروع
cd University_Major_Recommendation_flutter

# ثبت المكتبات
flutter pub get

# شغّل التطبيق
flutter run
```

---

**مبروك! 🎉 تطبيقك جاهز للعمل**

**Congratulations! 🎉 Your app is ready to run**

