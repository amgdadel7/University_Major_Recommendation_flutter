# إصلاح سريع لمشكلة الاتصال بالـ API

## المشكلة:
لا يمكن الاتصال بخادم API من جهاز Android حقيقي.

## الحل:

### 1. تأكد من أن الـ API يعمل:
```bash
cd university-major-recommendation-api
npm start
```

يجب أن ترى:
```
🚀 Server running on port 8000
📍 Network URL: http://10.0.0.66:8000/api/v1
```

### 2. العنوان تم تحديثه تلقائياً:
تم تحديث `lib/core/constants/api_config.dart` لاستخدام:
- `http://10.0.0.66:8000/api/v1` (للأجهزة الحقيقية)

### 3. إذا لم يعمل العنوان الأول:
افتح `lib/core/constants/api_config.dart` وغير السطر 34 إلى:
```dart
return 'http://10.5.0.2:8000/api/v1'; // جرب العنوان الثاني
```

### 4. التأكد من أن الحاسوب والجهاز على نفس الشبكة:
- تأكد من أن الحاسوب والجهاز Android على نفس شبكة WiFi
- جرب ping من الجهاز إلى الحاسوب للتأكد من الاتصال

### 5. جدار الحماية:
- تأكد من أن جدار الحماية يسمح بالاتصال على المنفذ 8000
- أو أطفئ جدار الحماية مؤقتاً للاختبار

### 6. إعادة تشغيل التطبيق:
بعد تحديث العنوان، أعد تشغيل التطبيق:
```bash
flutter run
```

## ملاحظات:
- إذا كان IP مختلف، استبدل `10.0.0.66` بـ IP حاسوبك
- يمكنك معرفة IP حاسوبك بـ: `ipconfig` (Windows) أو `ifconfig` (Mac/Linux)


