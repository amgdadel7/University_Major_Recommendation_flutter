# تحديث شاشة البداية (Splash Screen) - بوصلة الطالب

## ✅ التغييرات المطبقة

### 1. Android Splash Screen
- ✅ تم تحديث `launch_background.xml` بخلفية متدرجة بنفسجية (#6B46C1)
- ✅ تم إضافة أيقونة التطبيق في المركز
- ✅ تم إعداد مساحة لاسم التطبيق في الأسفل

### 2. iOS Launch Screen
- ✅ تم تحديث `LaunchScreen.storyboard` بإضافة نص "بوصلة الطالب"
- ✅ تم تغيير لون الخلفية إلى البنفسجي (#6B46C1)
- ✅ تم إضافة Label لعرض اسم التطبيق

### 3. Flutter Splash Page
- ✅ تم تحديث ملفات الترجمة:
  - العربية: "بوصلة الطالب"
  - الإنجليزية: "Student Compass"
- ✅ صفحة Splash تستخدم `'app_name'.tr()` لعرض الاسم تلقائياً

## 📱 النتيجة

بعد إعادة بناء التطبيق، ستظهر:
- **Android**: شاشة بداية بخلفية بنفسجية متدرجة مع أيقونة التطبيق
- **iOS**: شاشة بداية بنفسجية مع نص "بوصلة الطالب"
- **Flutter Splash**: صفحة Splash تعرض "بوصلة الطالب" / "Student Compass" حسب اللغة

## 🔄 الخطوات التالية

1. **تحويل SVG إلى PNG** (إذا لم تقم بذلك بعد):
   - استخدم [CloudConvert](https://cloudconvert.com/svg-to-png)
   - حول `assets/icons/app_icon.svg` إلى `assets/icons/app_icon.png` (1024x1024)

2. **إنشاء أيقونة Foreground**:
   - افتح `app_icon.png` واحذف الخلفية الدائرية
   - احفظ كـ `assets/icons/app_icon_foreground.png`

3. **توليد الأيقونات**:
   ```bash
   flutter pub get
   flutter pub run flutter_launcher_icons
   ```

4. **إعادة بناء التطبيق**:
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

## 📝 ملاحظات

- اسم التطبيق في شاشة البداية سيظهر تلقائياً من ملفات الترجمة
- في Android، الأيقونة ستظهر من `@mipmap/ic_launcher` (سيتم تحديثها بعد توليد الأيقونات)
- في iOS، النص ثابت "بوصلة الطالب" (يمكن تعديله لاحقاً لدعم اللغات)

