# دليل تغيير اسم التطبيق وإنشاء الأيقونة - بوصلة الطالب

## ✅ ما تم إنجازه

### 1. تغيير اسم التطبيق
- ✅ تم تحديث `pubspec.yaml` بالوصف الجديد
- ✅ تم إنشاء `android/app/src/main/res/values/strings.xml` للاسم بالعربية
- ✅ تم إنشاء `android/app/src/main/res/values-ar/strings.xml` للاسم بالعربية
- ✅ تم إنشاء `android/app/src/main/res/values-en/strings.xml` للاسم بالإنجليزية
- ✅ تم تحديث `ios/Runner/Info.plist` للاسم الجديد
- ✅ تم تحديث `AndroidManifest.xml` لاستخدام اسم التطبيق من strings.xml

### 2. إعداد الأيقونة
- ✅ تم إضافة `flutter_launcher_icons` في `pubspec.yaml`
- ✅ تم إنشاء تصميم SVG احترافي للأيقونة (`assets/icons/app_icon.svg`)
- ✅ تم إعداد الإعدادات في `pubspec.yaml` للأيقونة

## 📝 الخطوات المتبقية

### الخطوة 1: تحويل SVG إلى PNG

قم بتحويل ملف `assets/icons/app_icon.svg` إلى PNG بحجم 1024x1024 بكسل:

#### الطريقة 1: استخدام أدوات الإنترنت (الأسهل)
1. افتح [CloudConvert](https://cloudconvert.com/svg-to-png) أو [Convertio](https://convertio.co/svg-png/)
2. ارفع ملف `assets/icons/app_icon.svg`
3. اضبط الإعدادات:
   - Width: 1024
   - Height: 1024
   - Background: Transparent أو White
4. حمّل النتيجة واحفظها كـ `assets/icons/app_icon.png`

#### الطريقة 2: استخدام Inkscape (مجاني)
```bash
# تثبيت Inkscape من https://inkscape.org/
inkscape assets/icons/app_icon.svg --export-filename=assets/icons/app_icon.png --export-width=1024 --export-height=1024
```

#### الطريقة 3: استخدام ImageMagick
```bash
convert -background none -density 1024 assets/icons/app_icon.svg -resize 1024x1024 assets/icons/app_icon.png
```

### الخطوة 2: إنشاء أيقونة Adaptive Foreground

للأيقونة التكيفية في Android، تحتاج نسخة بدون خلفية دائرية:

1. افتح `app_icon.png` في محرر صور (GIMP, Photoshop, أو أي محرر)
2. احذف الخلفية الدائرية البنفسجية الخارجية
3. احرص على أن المحتوى المهم (البوصلة والرمز الأكاديمي) يبقى في المركز
4. احفظ الخلفية كـ **شفافة** (Transparent)
5. احفظ الملف كـ `assets/icons/app_icon_foreground.png`

**ملاحظة مهمة:** يجب أن يكون المحتوى المهم في منطقة مركزية 432x432 بكسل (للتأكد من ظهوره بشكل صحيح في Android)

### الخطوة 3: توليد الأيقونات

بعد تحضير الملفين PNG، قم بتنفيذ:

```bash
cd University_Major_Recommendation_flutter
flutter pub get
flutter pub run flutter_launcher_icons
```

سيتم إنشاء جميع أحجام الأيقونات المطلوبة لـ Android و iOS تلقائياً.

### الخطوة 4: إعادة بناء التطبيق

```bash
flutter clean
flutter pub get
flutter run
```

## 🎨 تصميم الأيقونة

تم تصميم الأيقونة لتشمل:
- **بوصلة احترافية**: تمثل التوجيه والمساعدة في إيجاد الاتجاه الصحيح
- **رمز أكاديمي**: قبعة تخرج تمثل الطالب والتعليم
- **ألوان احترافية**:
  - البنفسجي (#6B46C1): اللون الأساسي للتطبيق
  - الذهبي (#F59E0B): يرمز للإنجاز والأهداف
  - الأحمر (#EF4444): إبرة البوصلة الجنوبية
  - الأبيض: للوضوح والاحترافية

## 📱 النتيجة النهائية

بعد تنفيذ جميع الخطوات:
- ✅ اسم التطبيق: **بوصلة الطالب** (بالعربية) / **Student Compass** (بالإنجليزية)
- ✅ أيقونة احترافية في جميع المنصات (Android, iOS)
- ✅ أيقونة تكيفية للـ Android حديث

## 🔧 استكشاف الأخطاء

### المشكلة: الأيقونة لا تظهر بعد التنفيذ
**الحل:**
1. تأكد من وجود `app_icon.png` و `app_icon_foreground.png` في `assets/icons/`
2. تأكد من حجم الملفين (1024x1024 بكسل)
3. قم بتنفيذ `flutter clean` ثم `flutter pub get`
4. أعد بناء التطبيق

### المشكلة: اسم التطبيق لا يتغير
**الحل:**
1. تأكد من وجود ملف `strings.xml` في `android/app/src/main/res/values/`
2. تأكد من تحديث `AndroidManifest.xml` لاستخدام `@string/app_name`
3. قم بإعادة بناء التطبيق

## 📞 الدعم

إذا واجهت أي مشكلة، راجع:
- ملف `ICON_INSTRUCTIONS.md` في `assets/icons/`
- ملف `README.md` في `assets/icons/`

